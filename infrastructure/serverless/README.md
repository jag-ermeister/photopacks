# PhotoPacks.AI Serverless

### Deploy

- Export your `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` variables
- Deploy dev with `serverless deploy --stage dev`
- Deploy prod with `serverless deploy --stage prod`
